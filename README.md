# Marble Project

This repository contains the Marble Project front-end site — an interactive visualization and analysis of NYC education budget data.

Usage

Install dependencies and run the development server:

```bash
npm install
npm run dev
```

Build for production:

```bash
npm run build
npm run preview
```

The site is a static front-end application and does not require a backend to run.
